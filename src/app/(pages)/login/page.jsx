import { Login } from "@/components/index";
import React from "react";
export const metadata = {
  title: "Sign in",
  description: "Sign in your Postora account",
};
const page = () => {
  return <Login />;
};

export default page;
