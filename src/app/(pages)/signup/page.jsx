import { Signup } from "@/components/index";
import React from "react";
export const metadata = {
  title: "Sign Up",
  description: "Create a Postora account to share and explore images",
};
const page = () => {
  return <Signup />;
};

export default page;
